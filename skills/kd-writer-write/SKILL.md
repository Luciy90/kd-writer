---
name: kd-writer-write
description: "Прямой режим KD Writer для создания нового русскоязычного технического текста КД: технических требований, инструкций, ТЗ, ТУ, пояснительных и смежных документов. Использовать, когда пользователь явно просит написать новый документ или фрагмент с нуля."
---

# KD Writer — write

Используй канонический соседний скилл `../kd-writer/SKILL.md` и его `references/`. Применяй только операцию `write`.

Если канонический скилл недоступен, сообщи, что требуется установить полный набор KD Writer, и не импровизируй правила.

Не переключайся на `review`, `refactor` или `recreate` из-за содержимого исходника. Если пользователь просит другой режим, направь его к соответствующему entry skill.
